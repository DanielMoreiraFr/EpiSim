[PROMPT — CÓDIGO]
You are an expert software developer and epidemiologist. I need you to create a complete, self-contained interactive web application (single HTML file with embedded CSS and JavaScript — no external dependencies required) that simulates the spread of an infectious disease using the SIR epidemiological model (Susceptible → Infected → Recovered).
Project name: EpiSim — Infectious Disease Spread Simulator
Disease context: Dengue fever in a mid-sized Brazilian city (population ~500,000)
Requirements:

Implement the classic SIR differential equations numerically (Euler method or Runge-Kutta 4th order).
Allow the user to adjust the following parameters via sliders or input fields:

Total population (N)
Initial number of infected (I₀)
Basic reproduction number (R₀) — range 0.5 to 10
Infectious period in days (1/γ)
Simulation duration in days (up to 365)


Display a line chart showing S(t), I(t), and R(t) curves over time, using Chart.js loaded from CDN.
Show key computed metrics: peak number of infected, day of peak, total cases at end, herd immunity threshold.
The interface must be clean, professional, and responsive (mobile-friendly). Use a color scheme that is visually clear.
Add a brief explanation panel on the page describing what the SIR model is and what each parameter means, written in Portuguese (Brazil).
Include a comment block at the very top of the HTML file containing: the prompt used to generate this code, and the AI tool used.

Output: a single complete .html file ready to open in any browser with no internet connection required for the core simulation (Chart.js may use CDN).